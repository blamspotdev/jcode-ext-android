# Merged into the project's `gradle/libs.versions.toml`; an entry the catalogue already has is left
# alone. The Views stack, plus Navigation for the three top-level destinations.
[versions]
androidxAppcompat = "1.7.0"
androidxConstraintlayout = "2.2.0"
androidxNavigation = "2.8.5"
materialComponents = "1.12.0"

[libraries]
androidx-appcompat = { module = "androidx.appcompat:appcompat", version.ref = "androidxAppcompat" }
androidx-constraintlayout = { module = "androidx.constraintlayout:constraintlayout", version.ref = "androidxConstraintlayout" }
androidx-navigation-fragment-ktx = { module = "androidx.navigation:navigation-fragment-ktx", version.ref = "androidxNavigation" }
androidx-navigation-ui-ktx = { module = "androidx.navigation:navigation-ui-ktx", version.ref = "androidxNavigation" }
google-android-material = { module = "com.google.android.material:material", version.ref = "materialComponents" }
