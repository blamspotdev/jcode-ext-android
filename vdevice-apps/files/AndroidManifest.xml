<?xml version="1.0" encoding="utf-8"?>
<!-- The virtual device's file explorer, which is also its file and folder picker.

     Two jobs on purpose. A device wants somewhere to look at what is on it — until now the only way
     to see the device's storage was `adb ls` from outside — and an app that opens a document wants a
     picker. On a phone those are the same app, and making them the same app here means the screen
     that answers ACTION_OPEN_DOCUMENT is one somebody has actually used, rather than a dialog that
     only ever appears on somebody else's behalf.

     The SAF filters are what make it resolvable: an app that calls `resolveActivity` before offering
     an "attach a file" button needs something installed to find. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.blamspot.jcode.vdevice.files">

    <application
        android:label="Files"
        android:icon="@drawable/ic_launcher"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
        <activity
            android:name=".FilesActivity"
            android:exported="true"
            android:label="Files"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|density|smallestScreenSize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Opening an existing document, and the older GET_CONTENT that many apps still send. -->
            <intent-filter>
                <action android:name="android.intent.action.OPEN_DOCUMENT" />
                <action android:name="android.intent.action.GET_CONTENT" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.OPENABLE" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <!-- Naming a new one. -->
            <intent-filter>
                <action android:name="android.intent.action.CREATE_DOCUMENT" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.OPENABLE" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <!-- Choosing a folder for an app to work inside. -->
            <intent-filter>
                <action android:name="android.intent.action.OPEN_DOCUMENT_TREE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Opening a folder by name, which is how a "show me where that went" link arrives. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="resource/folder" />
                <data android:mimeType="vnd.android.document/directory" />
            </intent-filter>
        </activity>
    </application>

</manifest>
