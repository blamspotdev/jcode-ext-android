<?xml version="1.0" encoding="utf-8"?>
<!-- What the virtual device's hardware policy looks like from inside a guest.

     It asks for the three dangerous permissions the device governs, so that `checkSelfPermission`
     and `requestPermissions` are answered for an app that actually declares them — an app that does
     not is denied by the platform's own rules and would tell us nothing about the container. It
     requires none of them: every one is `required="false"` in effect, because the whole point is to
     run with the hardware switched off and report that it is off. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.blamspot.jcode.vdevice.hwfixture">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.compass" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false" />

    <!-- An action bar on purpose, and it is the second thing this fixture is for.

         Every other guest here declares NoActionBar, so nothing was checking that the container can
         host the piece of screen furniture the platform builds for an app rather than the app
         building it: the decor layout with a bar in it, the title the framework puts there, the
         options menu, and the overflow popup that menu opens in a window of its own. -->
    <application
        android:label="Hardware Fixture"
        android:icon="@drawable/ic_launcher"
        android:theme="@android:style/Theme.DeviceDefault">
        <activity
            android:name=".HardwareActivity"
            android:exported="true"
            android:label="Hardware Fixture"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|density|smallestScreenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
