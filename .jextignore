# The pack's Gradle modules. What ships is the archives they build -- lib/designer.apk,
# lib/sdkmanager.apk and lib/vdevice.apk, produced before packing, see README -- not the toolchain
# that builds them.
native
